<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="mapper.member">	<!-- member.xml의 네임스페이스 지정 -->
	<resultMap type="memberDTO" id="memResult">
	<!-- SQL문 실행한 후 반환되는 레코드들을 typeAlias에서 지정한 memberDTO 빈에 저장 -->
		<result property="id" column="id" /><!-- 레코드의 컬럼이름에 대해 memberDTO의 같은 속석에 값을 설정 -->
		<result property="pwd" column="pwd" />
		<result property="name" column="name" />
		<result property="email" column="email" />
		<result property="joinDate" column="joinDate" />
	</resultMap>
	
	<select id="selectAllMemberList" resultMap="memResult"> 
		<!-- id: dao에서 id를 이용해 해당 SQL 호출 -->
		<!-- resultMap: 반환되는 레코드를 memResult에 저장함 -->
		<![CDATA[
			SELECT * FROM T_MEMBER ORDER BY JOINDATE DESC
		]]>
		
	</select>
</mapper>